<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <style>
    body { font-family: sans-serif; padding: 20px; }
    .container { max-width: 800px; margin: 0 auto; }
    .form-group { margin-bottom: 15px; }
    .editor-container {
      display: flex;
      gap: 20px;
      margin-bottom: 15px;
    }
    .editor-container > div {
      flex: 1;
      display: flex;
      flex-direction: column;
    }
    #markdown-input {
      height: 300px;
      resize: vertical;
    }
    #markdown-preview {
      border: 1px solid #ccc;
      padding: 10px;
      height: 300px;
      overflow-y: auto;
      background-color: #f9f9f9;
    }
    label { display: block; margin-bottom: 5px; }
    input[type="text"], textarea { width: 100%; padding: 8px; box-sizing: border-box; }
    .btn { padding: 10px 15px; background-color: #007bff; color: white; border: none; cursor: pointer; text-decoration: none; display: inline-block; }
    .btn-cancel { background-color: #6c757d; }
  </style>
</head>
<body>
  <div class="container">
    <h1><%= title %></h1>

    <form action="/edit/<%= board.id %>" method="post">
      <div class="form-group">
        <label for="title">Title</label>
        <input type="text" id="title" name="title" value="<%= board.title %>" required>
      </div>
      <div class="editor-container">
        <div class="form-group">
          <label for="markdown-input">Content (Markdown)</label>
          <textarea id="markdown-input" name="message" required><%= board.message %></textarea>
        </div>
        <div class="form-group">
          <label for="markdown-preview">Preview</label>
          <div id="markdown-preview"></div>
        </div>
      </div>
      <button type="submit" class="btn">Update Post</button>
      <a href="/dashboard" class="btn btn-cancel">Cancel</a>
    </form>
  </div>

  <script src="/javascripts/markdown-it.min.js"></script>
  <script src="/javascripts/preview.js"></script>
</body>
</html>